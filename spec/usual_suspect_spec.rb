# frozen_string_literal: true

RSpec.describe UsualSuspect do
  it "has a version number" do
    expect(UsualSuspect::VERSION).not_to be nil
  end

  it "does something useful" do
    expect(false).to eq(true)
  end
end
